server:
  port: 8081

spring:
  application:
    name: pacienteMS
  
  datasource:
    url: jdbc:mysql://localhost:3306/pacientes_db
    username: root
    password: 
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  api-docs:
    enabled: true
    version: openapi_3_0
  swagger-ui:
    enabled: true