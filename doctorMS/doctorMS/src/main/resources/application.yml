server:
  port: 8082

spring:
  application:
    name: doctorMS # 👈 Nombre clave que usará Atenciones en su @FeignClient
  
  datasource:
    url: jdbc:mysql://localhost:3306/doctores_db
    username: root
    password: ""
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# 🔥 CONEXIÓN A EUREKA
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/